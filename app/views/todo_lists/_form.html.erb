<%= form_for(@todo_list, builder: FoundationFormBuilder) do |f| %>
	<%= f.label :title, class: "hide-text" %>
    <%= f.text_field :title, label: "New List", autofocus: 'autofocus' %>

    <%= f.submit "Save" %>

<% end %>
